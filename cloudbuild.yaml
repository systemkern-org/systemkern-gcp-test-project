# configuration for gcp cloud repo buil
steps:
- name: gcr.io/cloud-builders/gcloud
  args:
  - beta
  - functions
  - deploy
  - --trigger-http
  - --source=build/
  - --entry-point=f
  - helloWorld # Function name
